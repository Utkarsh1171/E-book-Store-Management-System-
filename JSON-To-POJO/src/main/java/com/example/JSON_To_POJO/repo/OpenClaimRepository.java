package com.example.JSON_To_POJO.repo;

import com.example.JSON_To_POJO.model.OpenClaim;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OpenClaimRepository extends JpaRepository<OpenClaim, Long> {
}
